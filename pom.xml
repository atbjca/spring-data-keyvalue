<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<groupId>cn.bjca.footstone.bpring.data</groupId>
	<artifactId>bjca-footstone-bpring-data-keyvalue</artifactId>
	<version>3.5.13-nes.patch.1</version>

	<name>Spring Data KeyValue</name>

	<parent>
		<groupId>org.springframework.data.build</groupId>
		<artifactId>spring-data-parent</artifactId>
		<version>3.5.13</version>
		<relativePath/>
	</parent>

	<properties>
		<springdata.commons>3.5.13-nes.patch.1</springdata.commons>
		<!-- NES fork 去特征化后的 Spring Framework 6.2 坐标版本（context/tx 及其传递依赖） -->
		<bpring.framework>6.2.19-nes.patch.1</bpring.framework>
		<java-module-name>spring.data.keyvalue</java-module-name>
	</properties>

	<dependencies>

		<dependency>
			<groupId>cn.bjca.footstone.bpring.data</groupId>
			<artifactId>bjca-footstone-bpring-data-commons</artifactId>
			<version>${springdata.commons}</version>
		</dependency>

		<dependency>
			<groupId>cn.bjca.footstone.bpring</groupId>
			<artifactId>bjca-footstone-bpring-context</artifactId>
			<version>${bpring.framework}</version>
		</dependency>

		<dependency>
			<groupId>cn.bjca.footstone.bpring</groupId>
			<artifactId>bjca-footstone-bpring-tx</artifactId>
			<version>${bpring.framework}</version>
		</dependency>

		<dependency>
			<groupId>com.querydsl</groupId>
			<artifactId>querydsl-collections</artifactId>
			<version>${querydsl}</version>
			<optional>true</optional>
		</dependency>

	</dependencies>

	<build>
		<plugins>

			<plugin>
				<groupId>com.mysema.maven</groupId>
				<artifactId>apt-maven-plugin</artifactId>
				<version>${apt}</version>
				<executions>
					<execution>
						<phase>generate-test-sources</phase>
						<goals>
							<goal>test-process</goal>
						</goals>
						<configuration>
							<outputDirectory>target/generated-test-sources</outputDirectory>
							<processor>com.querydsl.apt.QuerydslAnnotationProcessor</processor>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-assembly-plugin</artifactId>
			</plugin>

		</plugins>
	</build>

	<profiles>
		<profile>
			<!-- placeholder for no profile -->
			<id>none</id>
		</profile>
		<profile>
			<id>antora-process-resources</id>
			<build>
				<resources>
					<resource>
						<directory>src/main/antora/resources/antora-resources</directory>
						<filtering>true</filtering>
					</resource>
				</resources>
			</build>
		</profile>
		<profile>
			<id>antora</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.antora</groupId>
						<artifactId>antora-maven-plugin</artifactId>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

	<!--
		私服发布管理（NES 私服隔离）：制品发布指向内网 Nexus，URL 由 ~/.m2/settings.xml 的 bjca profile 提供，
		不硬编码。凭证 server id 对齐 releases / snapshots。
	-->
	<distributionManagement>
		<repository>
			<id>releases</id>
			<name>Nexus Release Repository</name>
			<url>${nexusReleaseUrl}</url>
		</repository>
		<snapshotRepository>
			<id>snapshots</id>
			<name>Nexus Snapshot Repository</name>
			<url>${nexusSnapshotUrl}</url>
		</snapshotRepository>
	</distributionManagement>

	<!--
		外网仓库隔离：移除官方 repo.spring.io 声明，依赖统一由内网私服 maven-public 聚合上游解析
		（mirror 配置见 ~/.m2/settings.xml）。
	-->

</project>
